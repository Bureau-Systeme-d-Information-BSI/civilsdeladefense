- resource_class = User
- provider = :france_connect

- resource_name = resource_class.name.underscore.to_sym
- devise_mapping = Devise.mappings[resource_name]

- if devise_mapping.omniauthable? && resource_class.omniauth_providers.include?(provider)
  .card
    .card-header
      .card-title= t('.title')
    .card-body
      - if current_user.link_to_omniauth?(provider: provider)
        = link_to t('.unlink_france_connect'), account_user_france_connect_link_path, method: :delete
      - else
        = render 'devise/shared/france_connect', show_or: false
