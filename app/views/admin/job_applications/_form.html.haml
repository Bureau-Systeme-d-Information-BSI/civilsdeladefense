= simple_form_for([:admin, @job_application]) do |f|
  = f.error_notification
  = f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present?

  .form-inputs
    = f.association :job_offer
    = f.association :user

  .form-actions
    = f.button :submit
