Snackbar.show({text: '<%= @notification %>', showAction: false})
window.boardRedraw()
<%- if @job_application.rejected? %>
var url = '<%= url_for([:emails, :admin, @job_offer, @job_application]) %>'
window.boardShowRejectionModal(url)
<%- end %>
