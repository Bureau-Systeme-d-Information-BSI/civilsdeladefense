Snackbar.show({text: '<%= @notification %>', showAction: false})
var nodeBoard = document.querySelector('#board')
if (nodeBoard !== null) {
  window.boardRedraw()
} else {
  window.reloadWithTurbo()
}
