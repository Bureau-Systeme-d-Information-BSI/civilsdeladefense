<p><%= t('.greeting', recipient: (@resource.full_name.present? ? @resource.full_name : nil)) %></p>

<p><%= t('.instruction') %></p>
<p><%= link_to t('.action'), confirmation_url(@resource, confirmation_token: @token) %></p>
