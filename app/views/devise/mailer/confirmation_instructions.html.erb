<p><%= t('.greeting', recipient: (@resource.name.present? ? @resource.name : nil)) %></p>

<p><%= t('.instruction') %></p>
<p><%= link_to t('.action'), confirmation_url(@resource, confirmation_token: @token) %></p>
