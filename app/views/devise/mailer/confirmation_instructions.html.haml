%p
  = t('.greeting', recipient: (@resource.full_name.present? ? @resource.full_name : nil), site_name: @resource.organization.name)
%p
  = t('.instruction')
- text = t('.action')
- url = confirmation_url(@resource, confirmation_token: @token)
= render partial: 'email_button', locals: {text: text, url: url}
