.fr-footer__bottom
  %ul.fr-footer__bottom-list
    - pages.each do |page|
      %li.fr-footer__bottom-item
        - if page.slug == "declaration-d-accessibilite"
          = link_to "Accessibilité : non conforme", page_path(page), class: 'fr-footer__bottom-link'
        - else
          = link_to page.title, page_path(page), class: 'fr-footer__bottom-link'
  .fr-footer__bottom-copy
    = copyright
